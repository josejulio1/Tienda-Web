/**
 * Constantes para los modales del panel de administración, para identificar en las clases ModalCreate
 * y ModalUpdate el funcionamiento de cada modal
 * @type {{CREATE: string, UPDATE: string}}
 * @author josejulio1
 * @version 1.0
 */
export const ModalType = {
    CREATE: 'CREATE',
    UPDATE: 'UPDATE'
}